# PostgreSQL 메모리 설정 (2GB 서버, 홈페이지 + 관리자 공용)
# postgresql.conf 맨 아래에 붙여 넣고 재시작: sudo systemctl restart postgresql
shared_buffers = 128MB
effective_cache_size = 512MB
work_mem = 4MB
maintenance_work_mem = 64MB
max_connections = 30
# 로컬 접속만 허용 (외부에서 DB에 직접 붙지 못하게)
listen_addresses = 'localhost'
